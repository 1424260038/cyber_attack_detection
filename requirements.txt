# 网络攻击检测系统依赖
# Cyber Attack Detection System Requirements

# ============ 核心依赖 (必须安装) ============
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
pyyaml>=6.0
tqdm>=4.65.0

# ============ API 服务 (可选) ============
fastapi>=0.115.0,<0.116.0
starlette>=0.37.2,<0.39.0
uvicorn>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.9

# ============ 可视化 (可选) ============
matplotlib>=3.7.0
seaborn>=0.12.0

# ============ 图神经网络 (可选，需要编译) ============
# networkx>=3.1
# torch-geometric>=2.3.0
# torch-scatter>=2.1.0
# torch-sparse>=0.6.17

# ============ 知识图谱 (可选) ============
# neo4j>=5.10.0

# ============ 图像处理 (可选) ============
# scikit-image>=0.21.0
# albumentations>=1.3.1

# ============ 其他工具 (可选) ============
# python-dotenv>=1.0.0
# scipy>=1.11.0
